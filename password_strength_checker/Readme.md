# Password Strength Checker

A Python-based password strength checker that uses the `rockyou.txt` wordlist to test passwords against common leaked passwords.

## Requirements

- Python 3
- `rockyou.txt` wordlist


